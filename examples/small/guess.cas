#import base
let number = randomInteger(100)
do {
	let guess = enterString("What number did I guess?")
	if(guess < number) {
		showMessage("Your number is too small!")
	} else if(guess > number) {
		showMessage("Your number is too big!")
	} else {
		showMessage("You are right!")
		end
	}
}

